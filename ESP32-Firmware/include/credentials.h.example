/**
 * WiFi and server credentials
 * 
 * Copy this file to credentials.h and fill in your values.
 * Add credentials.h to .gitignore to keep secrets out of git.
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi network
#define WIFI_SSID "YourWiFiSSID"
#define WIFI_PASSWORD "YourWiFiPassword"

// PC server IP address (run `ipconfig` on Windows to find it)
// Usually something like 192.168.1.xxx
#define SERVER_IP "192.168.1.100"
#define SERVER_PORT 5000

#endif
